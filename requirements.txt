# Core web framework for real-time communication
aiohttp==3.9.1
aiohttp-cors==0.7.0
websockets==12.0

# OpenAI GPT-4o for intelligent conversation
openai>=1.35.0

# Configuration management
python-dotenv==1.0.0

# Voice synthesis (female voice)
gTTS==2.4.0

# Speech recognition for wake words and conversation
SpeechRecognition==3.10.1

# Computer vision for camera awareness (headless for servers)
opencv-python-headless==4.8.1.78
Pillow==10.1.0
numpy==1.26.2

# Audio processing for ambient sound detection
librosa==0.10.1
soundfile==0.12.1

# Face detection and emotion recognition (lightweight)
mediapipe==0.10.8

# For better audio format handling
pydub==0.25.1

# HTTP client for external APIs
httpx==0.25.2
